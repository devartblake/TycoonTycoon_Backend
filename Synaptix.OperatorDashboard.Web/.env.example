# Backend API URL — leave empty for dev (uses Next.js proxy), set for production/Docker
NEXT_PUBLIC_API_URL=

# Admin Ops key — must match AdminOps:Key in the backend
NEXT_PUBLIC_ADMIN_OPS_KEY=CHANGE_ME
