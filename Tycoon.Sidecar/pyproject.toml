[project]
name = "tycoon-sidecar"
version = "1.0.0"
description = "Tycoon Backend sidecar — ML inference, analytics, webhooks, utilities"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
