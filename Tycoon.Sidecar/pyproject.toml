[project]
name = "synaptix-sidecar"
version = "1.0.0"
description = "Synaptix sidecar — ML inference, analytics, webhooks, and game balance utilities"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
